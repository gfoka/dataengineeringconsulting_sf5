{% extends 'admin.html.twig' %}

{% block title %}Edit SessionFormation{% endblock %}

{% block body %}
    <h1>Edit SessionFormation</h1>

    {{ include('session_formation/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_session_formation_index', {'id': session_formation.formation.id}) }}">back to list</a>

    {{ include('session_formation/_delete_form.html.twig') }}
{% endblock %}
