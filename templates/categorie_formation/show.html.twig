{% extends 'admin.html.twig' %}

{% block title %}CategorieFormation{% endblock %}

{% block body %}
    <h1>CategorieFormation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_formation.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ categorie_formation.libelle }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categorie_formation.description }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ categorie_formation.slug }}</td>
            </tr>
            <tr>
                <th>Ordre</th>
                <td>{{ categorie_formation.ordre }}</td>
            </tr>
            <tr>
                <th>Active</th>
                <td>{{ categorie_formation.active ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_formation_index') }}">back to list</a>

    <a href="{{ path('app_categorie_formation_edit', {'id': categorie_formation.id}) }}">edit</a>

    {{ include('categorie_formation/_delete_form.html.twig') }}
{% endblock %}
